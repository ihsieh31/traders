# Human-maintained top-level runtime dependency specification.
# Production/CI installs resolve through requirements.lock — a fully closed,
# exact-pinned closure generated from this file plus requirements-dev.txt via
# `uv pip compile --universal --python-version 3.11` (universal keeps
# environment markers, so the lock covers every platform CI/Docker install
# on, including the Linux-only akracer/py-mini-racer/greenlet pins akshare
# needs). Install closed and verify with:
#   pip install --no-deps -r requirements.lock
#   pip check
# Do not hand-edit requirements.lock; regenerate it whenever this file
# or requirements-dev.txt changes, and keep both in sync.
typing-extensions
openai>=2.33.0,<3.0.0
langchain>=0.3.27,<0.4.0
langchain-openai>=0.3.35,<0.4.0
langchain-core>=0.3.84,<1.0.0
langchain-anthropic>=0.3.22,<0.4.0
langchain-google-genai>=2.1.12,<3.0.0
langchain-experimental>=0.3.4,<0.4.0
numpy>=1.24.0
pandas>=2.0.0
yfinance
praw>=7.7.0
feedparser
stockstats>=0.5.4
eodhd
langgraph>=0.6.6,<0.7.0
langgraph-checkpoint-sqlite>=2.0.0,<3.0.0
chromadb
setuptools
backtrader
akshare
tushare
finnhub-python
parsel
requests
tqdm
pytz
redis
rich>=13.0.0
questionary>=2.0.1
typer>=0.9.0
python-dotenv
gradio>=4.0.0
plotly>=5.18.0
dash>=3.0.0
dash-bootstrap-components>=2.0.0
flask>=3.0.0
alpaca-py>=0.8.2
